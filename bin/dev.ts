import stream from "stream";


// cp.spawn() spawns process asynchronously
// cp.exec()  spawns shell and runs command within that shell
// cp.fork()  spawns a new node.js process and allows messaging between processes   